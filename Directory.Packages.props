<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>

  <ItemGroup>
    <!-- Composition / adapters -->
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.12" />
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection" Version="10.0.12" />
    <PackageVersion Include="Microsoft.Extensions.AI.Abstractions" Version="10.10.0" />
    <!-- The host's optional schema-intelligence extension, and the LLM live suite. Any
         OpenAI-compatible endpoint. -->
    <PackageVersion Include="Microsoft.Extensions.AI.OpenAI" Version="10.10.0" />
    <PackageVersion Include="M3L.Native" Version="0.10.0" />
    <PackageVersion Include="MorphDB.Client" Version="0.12.0" />
    <PackageVersion Include="Npgsql" Version="10.0.3" />
    <!-- Host: the HTTP surface and its OpenAPI document -->
    <PackageVersion Include="Microsoft.AspNetCore.OpenApi" Version="10.0.12" />
    <!--
      Pinned above the version Microsoft.AspNetCore.OpenApi resolves transitively: 2.0.0 carries
      GHSA-v5pm-xwqc-g5wc, and the vulnerability scan in CI fails the build on it. Stays on the
      2.x line on purpose: Microsoft.AspNetCore.OpenApi 10.0.x declares [2.7.5, 3.0.0), so the 3.x
      line cannot be taken until that package moves.
    -->
    <PackageVersion Include="Microsoft.OpenApi" Version="2.12.2" />
  </ItemGroup>

  <ItemGroup>
    <!-- Test -->
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.10.0" />
    <PackageVersion Include="xunit.v3" Version="4.0.1" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="4.0.0" />
    <!-- AwesomeAssertions: Apache-2.0 community fork of FluentAssertions (FA 8.x is commercial). API-compatible; namespace is AwesomeAssertions (not FluentAssertions). -->
    <PackageVersion Include="AwesomeAssertions" Version="9.6.0" />
    <PackageVersion Include="Testcontainers" Version="4.15.0" />
    <PackageVersion Include="Testcontainers.PostgreSql" Version="4.15.0" />
    <PackageVersion Include="Microsoft.AspNetCore.Mvc.Testing" Version="10.0.12" />
  </ItemGroup>
</Project>
